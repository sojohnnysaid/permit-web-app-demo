apiVersion: v1
kind: Service
metadata:
  name: tops-frontend
  labels:
    app: tops-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: tops-frontend
