apiVersion: v1
kind: Service
metadata:
  name: pmcsllc-frontend
  labels:
    app: pmcsllc-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: pmcsllc-frontend
